# Downloaded at install time — never committed (large native lib + vendored binding).
/third_party/

# Build artifacts
/src/kg
*.so
*.dylib
*.dll

# Local Go module cache noise
/src/vendor/

# Editor/OS
.DS_Store

# Local manual testbed (never committed)
/_testbed/

# Local dev review team (agents + /review) — not part of the shipped plugin
/.claude/
# kgai per-project knowledge graph store (own git/sync cycle)
.kgai/
# … except the fixture store the editor extension's tests read
!/editors/vscode/test/fixture/.kgai/
# … and the demo project's store, which ships with the repository
!/examples/acme-shop/.kgai/

# Local build artifacts of the readers
/src/kgread
/src/kgview
/dist/
